using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class GameRule {
	public static List<uint[]> Hint(uint[] hands,uint[] ids){
		var hints=new List<uint[]>();
		if(hands!=null&&ids!=null&&hands.Length>0&&ids.Length>0){

		}
		return hints;
	}
}
